# 05-05 PLAN — daily_report.py CLI (R10.8), reliability PNG, phase e2e test

<objective>
Close R10 with the human-facing surface and the ROADMAP's stated working test.
</objective>

<tasks>
- `performance.render_reliability_diagram()` — matplotlib Agg, empty bins skipped (D5-04)
- `src/analytics/daily_report.py` — `build_report()` plus a CLI with `--date`, `--snapshot`, `--plot`
- `tests/integration/test_phase5_e2e.py` — seed a week, render report, write snapshot + PNG,
  reconcile per-market-type breakdowns against the seeder manifest
</tasks>

<verification>
- Report renders on an empty database without printing raw "None"
- CLI exits 0 as a subprocess
- Every per-market-type figure reconciles with ground truth
- PNG has a valid header
</verification>
