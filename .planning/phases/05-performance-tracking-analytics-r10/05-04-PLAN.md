# 05-04 PLAN — recalibrate.py (R10.5) + snapshot.py (R10.7)

<objective>
Shadow recalibration that compares without deploying, and the versioned JSON snapshot
that becomes Phase 6's data contract.
</objective>

<tasks>
## recalibrate.py (R10.5, D5-01)
- `fit_candidate`, `compare_on_test_set`, `shadow_recalibrate`, `walk_forward_report`,
  `recalibrate_all`
- Must never call `ModelStore.save()` or any model's `train()` — asserted by tests that
  monkeypatch both to raise
- Compare on the test split; holdout never read

## snapshot.py (R10.7)
- `generate_snapshot` / `write_snapshot`, versioned via `SNAPSHOT_SCHEMA_VERSION`
- `win_rate_by_edge_bucket`, `recent_trades`, `model_versions`, `feature_importances`,
  `recent_improvements` (R11.3, R12.2)
- Every breakdown keyed by discovered values, never a fixed set (D5-07)
</tasks>

<verification>
- No-persist guarantee proven, not asserted in prose
- Snapshot is JSON-serialisable and valid on an empty database
- An unknown market type flows end-to-end into the snapshot
</verification>
