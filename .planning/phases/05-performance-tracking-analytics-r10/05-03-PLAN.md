# 05-03 PLAN — Feature importance (R10.4) + edge decay (R10.6)

<objective>
Two independent requirements sharing a plan because both are small and both feed the
snapshot in 05-04.
</objective>

<tasks>
## Feature importance (R10.4) — appended to performance.py per the CONVENTIONS analytics layout
- `gbm_feature_names(model_name)` — store name to ordered feature list
- `extract_feature_importances(model, names)` — average across the 3 CalibratedClassifierCV folds
- `record_feature_importances(db, ...)` — write to feature_importance (D5-02), idempotent per version
- `record_model_version(db, ...)` — populate the dead model_versions table (D5-02)
- `get_importance_history` / `importance_drift` — the "over time" half

## Edge decay (R10.6) — src/analytics/edge_decay.py
- `record_observation()` — the recorder hook, callable on every scan
- `decay_curve(ticker)`, `decay_by_bucket()`, `edge_persistence()`, `decay_by_market_type()`,
  `summarize_decay()`
</tasks>

<verification>
- Fold averaging asserted against a manual computation, not just structurally
- Feature-count mismatch raises rather than mislabelling
- Both decaying and persistent edge populations detected in seeded data
- cbb_games reaches the decay breakdown
</verification>
