# 05-02 PLAN — performance.py: Brier, calibration, P&L, Sharpe

<objective>
The core metrics module. Everything downstream (snapshot, daily report, evaluator)
reads through it.
</objective>

<tasks>
- `_settled_predictions()` — the single `predictions` <-> `outcomes` join (D5-06), with the
  ticker fallback for rows whose `prediction_id` is NULL.
- R10.1: `brier_summary`, `brier_by_market_type`, `brier_by_model`, `rolling_brier` (last 50/100).
- R10.2: `calibration_bins()` returning binned data, fixed shape, empty bins as None (D5-04).
- R10.3: `pnl_curve`, `win_rate`, `edge_on_wins_vs_losses`, `pnl_by_market_type`, `drawdown`,
  `sharpe_ratio` (daily returns, sqrt(252), D5-05).
- `recompute_daily_pnl` / `recompute_all_daily_pnl` — the aggregator that never existed.
- Market types discovered via `SELECT DISTINCT`, never enumerated (D5-07).
- Reuse `src.validation.metrics` for scalars so analytics and the validator cannot drift.
</tasks>

<verification>
- Metrics asserted against hand-computed values, not just types
- Empty slices return None rather than raising
- Analytics-computed drift matches the seeder manifest exactly
- `cbb_games` appears in every breakdown
</verification>
