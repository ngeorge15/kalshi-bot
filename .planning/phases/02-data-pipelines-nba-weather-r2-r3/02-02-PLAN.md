---
phase: 02-data-pipelines-nba-weather-r2-r3
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data/nba/teams.py
  - src/data/nba/history.py
  - tests/data/test_nba_teams.py
  - tests/data/test_nba_history.py
autonomous: true
requirements:
  - R2.1
  - R2.4
  - R2.6
  - R2.7
  - R2.8
must_haves:
  truths:
    - "get_team_stats() returns all 30 NBA teams with OFF_RATING, DEF_RATING, NET_RATING, PACE"
    - "get_todays_schedule() returns list of games with HOME_TEAM_ID and VISITOR_TEAM_ID"
    - "ELO updates correctly: winner ELO increases, loser ELO decreases"
    - "get_rest_days(team_id) returns correct days since last game"
    - "fetch_historical_results() writes game records to nba_game_results SQLite table"
    - "All nba_api calls include time.sleep(0.5) rate-limit delay"
    - "API failures serve stale cache with warning log (D-04)"
  artifacts:
    - path: "src/data/nba/teams.py"
      provides: "Team stats, schedule, ELO, rest tracking"
      exports: ["get_team_stats", "get_todays_schedule", "get_rest_days", "get_team_elos", "update_elo", "EloTracker"]
    - path: "src/data/nba/history.py"
      provides: "Historical game results to SQLite"
      exports: ["fetch_historical_results", "get_historical_results"]
    - path: "tests/data/test_nba_teams.py"
      provides: "Unit tests for teams module"
    - path: "tests/data/test_nba_history.py"
      provides: "Unit tests for history module"
  key_links:
    - from: "src/data/nba/teams.py"
      to: "src/data/cache.py"
      via: "cache_get/cache_set wrapping nba_api calls"
      pattern: "cache_get.*team_stats"
    - from: "src/data/nba/history.py"
      to: "src/db/database.py"
      via: "Database.execute for INSERT into nba_game_results"
      pattern: "db\\.execute.*nba_game_results"
---

<objective>
Build the NBA team stats, schedule, ELO rating, rest tracking, and historical results pipeline. This covers team-level data that the player module and future models will consume.

Purpose: Team stats (ratings, pace) and ELO are core features for the NBA game prediction model (Phase 3). Historical results provide training data. Schedule drives which players to fetch stats for (D-05).
Output: Working teams.py and history.py with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-data-pipelines-nba-weather-r2-r3/02-CONTEXT.md
@.planning/phases/02-data-pipelines-nba-weather-r2-r3/02-RESEARCH.md
@.planning/phases/02-data-pipelines-nba-weather-r2-r3/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/data/cache.py (created in Plan 01):
```python
def cache_get(namespace: str, params: dict, ttl_seconds: int | None = None) -> dict | None: ...
def cache_set(namespace: str, params: dict, value: dict) -> None: ...
def cache_get_stale(namespace: str, params: dict) -> dict | None: ...
```

From src/db/database.py:
```python
class Database:
    def __init__(self, db_path: str = "data/kalshi_bot.db") -> None: ...
    def execute(self, sql: str, params: tuple = ()) -> sqlite3.Cursor: ...
    def fetchall(self, sql: str, params: tuple = ()) -> list[dict[str, Any]]: ...
    def fetchone(self, sql: str, params: tuple = ()) -> dict[str, Any] | None: ...
```

From src/config.py:
```python
class Config:
    cache: dict  # {"ttl_seconds": {"team_stats": 86400, "schedule": 86400, ...}}
    markets: dict  # {"nba": {"player_trend_games": 5, ...}, ...}
config: Config | None
```

Schema (from schema.sql):
```sql
CREATE TABLE IF NOT EXISTS nba_game_results (
    id INTEGER PRIMARY KEY, game_id TEXT NOT NULL UNIQUE, game_date TEXT NOT NULL,
    home_team_id INTEGER NOT NULL, away_team_id INTEGER NOT NULL,
    home_pts INTEGER NOT NULL, away_pts INTEGER NOT NULL,
    home_win INTEGER NOT NULL, season TEXT NOT NULL,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

nba_api patterns (from RESEARCH.md):
- LeagueDashTeamStats: `season="2024-25"`, `measure_type_detailed_defense="Advanced"` -> `result["LeagueDashTeamStats"]`
- ScoreboardV3: `game_date=date.today().strftime("%m/%d/%Y")`, `league_id="00"` -> `result["GameHeader"]`
- LeagueGameFinder: `player_or_team_abbreviation="T"`, `season_nullable="2023-24"` -> `result["LeagueGameFinderResults"]`
- CRITICAL: time.sleep(0.5) before EVERY nba_api call
- CRITICAL: Use ScoreboardV3, NOT ScoreboardV2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NBA teams module — stats, schedule, ELO, rest tracking</name>
  <files>src/data/nba/teams.py, tests/data/test_nba_teams.py</files>
  <read_first>
    src/data/cache.py,
    src/config.py,
    src/db/database.py,
    src/kalshi/client.py,
    tests/conftest.py,
    tests/data/conftest.py,
    .planning/phases/02-data-pipelines-nba-weather-r2-r3/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: get_team_stats() returns list of dicts with keys TEAM_ID, TEAM_NAME, OFF_RATING, DEF_RATING, NET_RATING, PACE, W, L, W_PCT (mock nba_api)
    - Test: get_team_stats() uses cache — second call with valid TTL does NOT call nba_api again
    - Test: get_team_stats() on API failure returns stale cache and logs warning (D-04)
    - Test: get_todays_schedule() returns list of dicts with GAME_ID, HOME_TEAM_ID, VISITOR_TEAM_ID, GAME_DATE_EST (mock ScoreboardV3)
    - Test: get_rest_days(team_id, schedule_data) returns integer days since last game; 0 for back-to-back
    - Test: EloTracker.update(winner_id, loser_id, margin) increases winner ELO and decreases loser ELO
    - Test: EloTracker.expected_outcome() returns ~0.5 for equal ELOs, >0.5 when team A is higher rated
    - Test: EloTracker season reset pulls ratings toward 1300 by 1/3
    - Test: EloTracker.get_elo(team_id) returns INITIAL_ELO (1300) for unknown team
  </behavior>
  <action>
    **Create `tests/data/test_nba_teams.py`** first (RED phase). Mock nba_api endpoints using `monkeypatch` to replace the endpoint classes' `get_normalized_dict()` return values. Do NOT use `responses` library for nba_api mocking — mock at the endpoint class level.

    **Create `src/data/nba/teams.py`** (GREEN phase) with:

    1. **`get_team_stats(season: str = "2024-25") -> list[dict]`**
       - Cache namespace: `"team_stats"`, params: `{"season": season}`
       - Call `LeagueDashTeamStats(season=season, season_type_all_star="Regular Season", measure_type_detailed_defense="Advanced", per_mode_simple="PerGame").get_normalized_dict()`
       - Return `result["LeagueDashTeamStats"]`
       - Wrap in try/except: on failure, call `cache_get_stale("team_stats", params)` and `logger.warning("nba_api failed for team stats, serving stale cache")`
       - `time.sleep(0.5)` before the nba_api call

    2. **`get_todays_schedule(game_date: date | None = None) -> list[dict]`**
       - Default to `date.today()` if None
       - Cache namespace: `"schedule"`, params: `{"date": game_date.isoformat()}`
       - Call `ScoreboardV3(game_date=game_date.strftime("%m/%d/%Y"), league_id="00").get_normalized_dict()`
       - Return `result["GameHeader"]`
       - Same try/except + stale fallback pattern
       - `time.sleep(0.5)` before the nba_api call
       - **CRITICAL: Use ScoreboardV3, NOT ScoreboardV2**

    3. **`get_rest_days(team_id: int, schedule: list[dict], reference_date: date | None = None) -> int`**
       - Find most recent game where team was home or away before reference_date
       - Return number of days since that game. If no previous game found, return -1 (unknown).
       - Back-to-back = 0 or 1 day rest.

    4. **`class EloTracker`**:
       - `__init__(self)`: `self._ratings: dict[int, float] = {}` (team_id -> ELO)
       - Constants: `K_BASE = 20`, `INITIAL_ELO = 1300`, `HOME_ADVANTAGE = 100`, `SEASON_RESET_FRACTION = 0.33`
       - `get_elo(team_id: int) -> float`: return rating or INITIAL_ELO
       - `expected_outcome(elo_a: float, elo_b: float) -> float`: `1.0 / (1.0 + 10 ** ((elo_b - elo_a) / 400))`
       - `_elo_k(mov: int, elo_diff: float) -> float`: FiveThirtyEight MOV-adjusted K: `K_BASE * ((mov + 3) ** 0.8) / (7.5 + 0.006 * abs(elo_diff))`
       - `update(winner_id: int, loser_id: int, margin: int) -> None`: compute expected, compute K, update both ratings
       - `season_reset() -> None`: for each rating, `new = old - (old - 1300) * 0.33`
       - `get_all_ratings() -> dict[int, float]`: return copy of _ratings

    Module-level: `logger = logging.getLogger(__name__)`
    Use absolute imports: `from src.data.cache import cache_get, cache_set, cache_get_stale`
  </action>
  <verify>
    <automated>cd /Users/nikhi/Documents/Projects/kalshi-bot && python -m pytest tests/data/test_nba_teams.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/data/nba/teams.py contains `def get_team_stats(`
    - src/data/nba/teams.py contains `def get_todays_schedule(`
    - src/data/nba/teams.py contains `def get_rest_days(`
    - src/data/nba/teams.py contains `class EloTracker`
    - src/data/nba/teams.py contains `ScoreboardV3` (NOT ScoreboardV2)
    - src/data/nba/teams.py contains `time.sleep(0.5)`
    - src/data/nba/teams.py contains `cache_get_stale` (stale fallback per D-04)
    - src/data/nba/teams.py contains `logger.warning` (log stale fallback per D-04)
    - src/data/nba/teams.py contains `INITIAL_ELO = 1300`
    - src/data/nba/teams.py contains `HOME_ADVANTAGE = 100`
    - src/data/nba/teams.py contains `SEASON_RESET_FRACTION = 0.33`
    - tests/data/test_nba_teams.py has at least 8 test functions
    - tests/data/test_nba_teams.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    Team stats, schedule, ELO, and rest tracking functions all tested. Cache integration and stale fallback verified. ScoreboardV3 used correctly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NBA historical results — LeagueGameFinder to SQLite</name>
  <files>src/data/nba/history.py, tests/data/test_nba_history.py</files>
  <read_first>
    src/data/nba/teams.py,
    src/data/cache.py,
    src/db/database.py,
    src/db/schema.sql,
    .planning/phases/02-data-pipelines-nba-weather-r2-r3/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: fetch_historical_results() calls LeagueGameFinder for 3 seasons (2021-22, 2022-23, 2023-24) and writes to nba_game_results table
    - Test: fetch_historical_results() deduplicates — calling twice does not double records (game_id UNIQUE constraint)
    - Test: get_historical_results(season=None) returns all stored results as list[dict]
    - Test: get_historical_results(season="2023-24") filters by season
    - Test: results stored with correct home_win flag (1 if home_pts > away_pts, 0 otherwise)
    - Test: MATCHUP parsing correctly identifies home vs away ("LAL vs. GSW" = LAL home, "LAL @ GSW" = LAL away)
    - Test: cache with "historical" namespace used (TTL 604800 = 7 days)
  </behavior>
  <action>
    **Create `tests/data/test_nba_history.py`** first (RED). Use in-memory SQLite (`Database(":memory:")`) for isolation. Mock `LeagueGameFinder.get_normalized_dict()` to return sample game data.

    **Create `src/data/nba/history.py`** (GREEN) with:

    1. **`fetch_historical_results(db: Database | None = None, seasons: list[str] | None = None) -> int`**
       - Default seasons: `["2021-22", "2022-23", "2023-24"]` (3 seasons per research recommendation)
       - For each season:
         - `time.sleep(0.5)` then call `LeagueGameFinder(player_or_team_abbreviation="T", season_nullable=season, season_type_nullable="Regular Season").get_normalized_dict()`
         - Result key: `"LeagueGameFinderResults"`
         - Each row has: GAME_ID, GAME_DATE, TEAM_ID, TEAM_ABBREVIATION, MATCHUP, WL, PTS
         - Group by GAME_ID (each game appears twice — once per team)
         - Parse MATCHUP: if "vs." present, that team is home. If "@" present, that team is away.
         - INSERT OR IGNORE into `nba_game_results` table: game_id, game_date, home_team_id, away_team_id, home_pts, away_pts, home_win (1 if home_pts > away_pts else 0), season
       - Cache namespace: `"historical"`, params: `{"type": "nba_results", "seasons": seasons}`
       - On success, `cache_set` the fetch timestamp
       - Use `db or Database()` for database access
       - Return count of newly inserted rows
       - Wrap nba_api calls in try/except with stale cache fallback and warning log

    2. **`get_historical_results(db: Database | None = None, season: str | None = None) -> list[dict]`**
       - If season is None, `SELECT * FROM nba_game_results ORDER BY game_date`
       - If season provided, `SELECT * FROM nba_game_results WHERE season = ? ORDER BY game_date`
       - Return list of dicts

    Module-level: `logger = logging.getLogger(__name__)`
  </action>
  <verify>
    <automated>cd /Users/nikhi/Documents/Projects/kalshi-bot && python -m pytest tests/data/test_nba_history.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/data/nba/history.py contains `def fetch_historical_results(`
    - src/data/nba/history.py contains `def get_historical_results(`
    - src/data/nba/history.py contains `LeagueGameFinder`
    - src/data/nba/history.py contains `time.sleep(0.5)`
    - src/data/nba/history.py contains `INSERT OR IGNORE INTO nba_game_results`
    - src/data/nba/history.py contains `"2021-22"` and `"2022-23"` and `"2023-24"`
    - src/data/nba/history.py contains `vs.` or `vs` (MATCHUP home detection)
    - tests/data/test_nba_history.py has at least 6 test functions
    - tests/data/test_nba_history.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    Historical results pipeline fetches 3 seasons via LeagueGameFinder, correctly parses home/away from MATCHUP string, stores to SQLite with deduplication, and reads back with optional season filter.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/data/test_nba_teams.py tests/data/test_nba_history.py -x -q` — all NBA tests pass
- `python -c "from src.data.nba.teams import get_team_stats, get_todays_schedule, EloTracker; print('OK')"` — imports work
- `python -c "from src.data.nba.history import fetch_historical_results, get_historical_results; print('OK')"` — imports work
- `python -m pytest tests/ -x -q` — full suite passes (no regressions)
</verification>

<success_criteria>
- Team stats fetch returns OFF_RATING, DEF_RATING, NET_RATING, PACE for all teams
- Schedule fetch uses ScoreboardV3 (not V2) and returns home/away team IDs
- ELO tracker correctly updates ratings and supports season reset
- Rest day calculation works for back-to-back detection
- Historical results populate nba_game_results table from 3 seasons
- All API calls cache responses and serve stale data on failure
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipelines-nba-weather-r2-r3/02-02-SUMMARY.md`
</output>
