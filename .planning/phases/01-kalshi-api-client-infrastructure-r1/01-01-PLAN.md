---
phase: 01-kalshi-api-client-infrastructure-r1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - .env.example
  - trading_config.json
  - pytest.ini
  - src/__init__.py
  - src/kalshi/__init__.py
  - src/db/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_kalshi_client.py
autonomous: true
requirements:
  - R1.10

must_haves:
  truths:
    - "pytest collects all test stubs without error"
    - "Project directory structure matches CONVENTIONS.md"
    - "requirements.txt lists all pinned dependencies"
    - ".env.example documents all required environment variables"
  artifacts:
    - path: "tests/conftest.py"
      provides: "RSA key fixture, mock config fixture"
      contains: "test_private_key_path"
    - path: "tests/test_kalshi_client.py"
      provides: "All unit + integration test stubs"
      contains: "test_auth_signature"
    - path: "pytest.ini"
      provides: "pytest config with integration marker"
      contains: "integration"
    - path: "trading_config.json"
      provides: "Default trading configuration"
      contains: "kelly_fraction"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "KALSHI_API_KEY_ID"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_kalshi_client.py"
      via: "pytest fixture injection"
      pattern: "test_private_key_path"
---

<objective>
Create the project skeleton and Wave 0 test scaffolding for Phase 1.

Purpose: Establish directory structure per CONVENTIONS.md, install dependencies, create all package init files, and scaffold every test stub from the VALIDATION strategy so that all subsequent plans have tests to run against from their first commit.

Output: Complete project skeleton with pytest collecting all stubs, requirements.txt with pinned deps, .env.example, trading_config.json, .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/CONVENTIONS.md
@.planning/phases/01-kalshi-api-client-infrastructure-r1/01-CONTEXT.md
@.planning/phases/01-kalshi-api-client-infrastructure-r1/01-RESEARCH.md
@.planning/phases/01-kalshi-api-client-infrastructure-r1/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton — directories, config files, .gitignore, requirements.txt</name>
  <files>
    .gitignore,
    requirements.txt,
    .env.example,
    trading_config.json,
    src/__init__.py,
    src/kalshi/__init__.py,
    src/db/__init__.py,
    tests/__init__.py
  </files>
  <read_first>
    .planning/CONVENTIONS.md,
    .planning/phases/01-kalshi-api-client-infrastructure-r1/01-RESEARCH.md
  </read_first>
  <action>
1. Create all directories: `src/`, `src/kalshi/`, `src/db/`, `tests/`, `data/`, `data/cache/`, `data/models/`.

2. Create empty `__init__.py` files in: `src/`, `src/kalshi/`, `src/db/`, `tests/`.

3. Create `.gitignore` with these exact entries:
```
.env
data/
__pycache__/
*.pyc
.venv/
*.pem
.pytest_cache/
*.egg-info/
dist/
build/
```

4. Create `requirements.txt` with pinned versions (per RESEARCH.md verified versions):
```
requests==2.32.3
cryptography==43.0.0
python-dotenv>=1.0.1
pytest==7.4.4
pytest-mock==3.15.1
responses==0.26.0
```

5. Create `.env.example` with all required env vars (per CONVENTIONS.md):
```
# Kalshi
KALSHI_API_KEY_ID=your-key-id
KALSHI_PRIVATE_KEY_PATH=./kalshi_private_key.pem
KALSHI_ENV=demo

# Evaluator LLM (Gemini is default; supports Claude/OpenAI via EVALUATOR_LLM_PROVIDER)
EVALUATOR_LLM_PROVIDER=gemini
GOOGLE_API_KEY=your-gemini-api-key

# Optional: if switching evaluator to Claude or OpenAI
# EVALUATOR_LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-...
# EVALUATOR_LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...

# NWS (optional — for User-Agent header)
NWS_CONTACT_EMAIL=your@email.com
```

6. Create `trading_config.json` with the exact JSON from CONVENTIONS.md Config File Structure section (the full JSON with environment, markets.nba, markets.weather, risk, and validation sections).

7. Run `pip install -r requirements.txt` to install all dependencies.
  </action>
  <verify>
    <automated>python -c "import requests, cryptography, dotenv, pytest; print('deps ok')" && test -f .gitignore && test -f requirements.txt && test -f .env.example && test -f trading_config.json && test -f src/__init__.py && test -f src/kalshi/__init__.py && test -f src/db/__init__.py && test -f tests/__init__.py && echo "skeleton ok"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `.env` on its own line
    - `.gitignore` contains `data/` on its own line
    - `.gitignore` contains `*.pem` on its own line
    - `requirements.txt` contains `requests==2.32.3`
    - `requirements.txt` contains `cryptography==43.0.0`
    - `requirements.txt` contains `python-dotenv>=1.0.1`
    - `.env.example` contains `KALSHI_API_KEY_ID=`
    - `.env.example` contains `KALSHI_PRIVATE_KEY_PATH=`
    - `.env.example` contains `KALSHI_ENV=demo`
    - `trading_config.json` contains `"kelly_fraction": 0.25`
    - `trading_config.json` contains `"max_daily_loss_cents": -10000`
    - `src/__init__.py` exists (can be empty)
    - `src/kalshi/__init__.py` exists (can be empty)
    - `src/db/__init__.py` exists (can be empty)
    - `tests/__init__.py` exists (can be empty)
  </acceptance_criteria>
  <done>All skeleton files exist, dependencies installed, directory structure matches CONVENTIONS.md</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolding — conftest.py, test stubs, pytest.ini</name>
  <files>
    pytest.ini,
    tests/conftest.py,
    tests/test_kalshi_client.py
  </files>
  <read_first>
    .planning/phases/01-kalshi-api-client-infrastructure-r1/01-VALIDATION.md,
    .planning/phases/01-kalshi-api-client-infrastructure-r1/01-RESEARCH.md
  </read_first>
  <action>
1. Create `pytest.ini`:
```ini
[pytest]
markers =
    integration: marks tests as requiring live Kalshi demo API (deselect with '-m "not integration"')
testpaths = tests
```

2. Create `tests/conftest.py` with these fixtures:
   - `test_private_key_path` (scope="session"): Generate a fresh 2048-bit RSA key using `cryptography.hazmat.primitives.asymmetric.rsa.generate_private_key(public_exponent=65537, key_size=2048)`. Serialize to PEM with `TraditionalOpenSSL` format, `NoEncryption()`. Write to a tempdir file. Return the path as a string. Use `tmp_path_factory.mktemp("keys")`.
   - `mock_config_env` (scope="function"): A fixture that uses `monkeypatch` to set `KALSHI_API_KEY_ID=test-key-id`, `KALSHI_PRIVATE_KEY_PATH` to the `test_private_key_path` fixture value, and `KALSHI_ENV=demo`. This fixture should accept `test_private_key_path` as a parameter.
   - `skip_without_integration`: An autouse=False fixture that skips tests if `KALSHI_INTEGRATION` env var is not set to `true`. Use `pytest.skip("Set KALSHI_INTEGRATION=true to run")`.

3. Create `tests/test_kalshi_client.py` with ALL of the following test function stubs. Each stub should have a docstring describing what it tests and a body of `pass` (so pytest collects but does not fail):

Unit test stubs (no marker needed):
- `test_auth_signature` — "KalshiAuth produces valid base64 RSA-PSS signature for timestamp+method+path"
- `test_auth_headers` — "KalshiAuth sets KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE, KALSHI-ACCESS-TIMESTAMP headers"
- `test_auth_strips_query_string` — "KalshiAuth signs path without query string (path only, not ?foo=bar)"
- `test_base_url_demo` — "Config uses https://demo-api.kalshi.co/trade-api/v2 when KALSHI_ENV=demo"
- `test_base_url_prod` — "Config uses https://trading-api.kalshi.com/trade-api/v2 when KALSHI_ENV=production"
- `test_market_discovery_pagination` — "get_markets_by_series follows cursor pagination across multiple pages"
- `test_get_event` — "get_event returns event dict with markets list"
- `test_orderbook_parse` — "get_orderbook parses _dollars format into cents tuples"
- `test_place_order_payload` — "place_limit_order sends correct JSON payload with ticker, side, price, count"
- `test_cancel_order` — "cancel_order calls DELETE /portfolio/orders/{order_id}"
- `test_get_balance` — "get_balance returns balance_cents and portfolio_value_cents"
- `test_get_positions` — "get_positions returns list of position dicts"
- `test_get_fills` — "get_fills returns list of fill dicts with _dollars fields"
- `test_retry_config` — "Session has Retry adapter mounted with total=3, backoff_factor=1"
- `test_missing_env_var` — "Config raises error when KALSHI_API_KEY_ID is missing"
- `test_dollars_to_cents` — "dollars_to_cents('0.6500') returns 65"

Integration test stubs (all marked with `@pytest.mark.integration`):
- `test_integration_auth` — "Authenticate to demo API and get exchange status"
- `test_integration_discover_nba_games` — "Discover NBA game markets via series_ticker=KXNBAGAME"
- `test_integration_orderbook` — "Pull orderbook for a real market ticker"
- `test_integration_place_cancel` — "Place a limit order at extreme price and cancel it"
- `test_integration_get_balance` — "Get demo account balance"
- `test_integration_get_positions` — "Get demo account positions"

Each integration test stub should also call `skip_without_integration` fixture or use `pytestmark = pytest.mark.integration` at the class/module level for the integration section. Group integration tests in a class `TestIntegration` with `pytestmark = pytest.mark.integration`.
  </action>
  <verify>
    <automated>pytest tests/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest.ini` contains `integration: marks tests`
    - `tests/conftest.py` contains `def test_private_key_path(`
    - `tests/conftest.py` contains `generate_private_key(public_exponent=65537, key_size=2048)`
    - `tests/conftest.py` contains `def mock_config_env(`
    - `tests/test_kalshi_client.py` contains `def test_auth_signature`
    - `tests/test_kalshi_client.py` contains `def test_auth_headers`
    - `tests/test_kalshi_client.py` contains `def test_auth_strips_query_string`
    - `tests/test_kalshi_client.py` contains `def test_base_url_demo`
    - `tests/test_kalshi_client.py` contains `def test_base_url_prod`
    - `tests/test_kalshi_client.py` contains `def test_market_discovery_pagination`
    - `tests/test_kalshi_client.py` contains `def test_orderbook_parse`
    - `tests/test_kalshi_client.py` contains `def test_place_order_payload`
    - `tests/test_kalshi_client.py` contains `def test_cancel_order`
    - `tests/test_kalshi_client.py` contains `def test_get_balance`
    - `tests/test_kalshi_client.py` contains `def test_retry_config`
    - `tests/test_kalshi_client.py` contains `def test_missing_env_var`
    - `tests/test_kalshi_client.py` contains `def test_dollars_to_cents`
    - `tests/test_kalshi_client.py` contains `class TestIntegration`
    - `tests/test_kalshi_client.py` contains `def test_integration_auth`
    - `tests/test_kalshi_client.py` contains `def test_integration_place_cancel`
    - `pytest --collect-only` collects 22 tests (16 unit + 6 integration) with 0 errors
  </acceptance_criteria>
  <done>pytest collects all 22 test stubs without errors, conftest provides RSA key and mock config fixtures, integration marker registered</done>
</task>

</tasks>

<verification>
- `pytest tests/ --collect-only -q` collects 22 tests with no errors
- `pytest tests/ -v -m "not integration"` runs 16 unit tests, all pass (stubs are `pass`)
- All files in CONVENTIONS.md directory structure exist
- `trading_config.json` is valid JSON matching CONVENTIONS.md schema
</verification>

<success_criteria>
- Project directory structure matches CONVENTIONS.md Phase 1 subset
- All dependencies install cleanly
- pytest collects all test stubs without error
- .env.example documents every required environment variable
- trading_config.json contains complete default configuration
</success_criteria>

<output>
After completion, create `.planning/phases/01-kalshi-api-client-infrastructure-r1/01-01-SUMMARY.md`
</output>
